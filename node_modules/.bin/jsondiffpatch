#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@bigfunger/jsondiffpatch/bin/jsondiffpatch" "$@"
  ret=$?
else 
  node  "$basedir/../@bigfunger/jsondiffpatch/bin/jsondiffpatch" "$@"
  ret=$?
fi
exit $ret
